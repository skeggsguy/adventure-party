# local Claude Code settings (and dated backups of them)
.claude/settings.local.json
.claude/settings.local.json.bak-*

# local scratch backlog of open follow-ups (not experience, not shipped)
.claude/notes.md

# local dogfood launchers
party.bat
party.sh
ts_note.txt

# local working notes for the session-zero rework (not shipped)
todo-session0.md

# local test-app sandbox for dogfooding the plugin
tennis/

# leftover per-user XP-display copy from setups before 0.7.0 dropped it
.claude/party/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Windows "downloaded from the internet" metadata sidecars
*:Zone.Identifier
*Zone.Identifier
