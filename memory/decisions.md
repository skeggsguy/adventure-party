# Decisions

Why A over B, for choices below plan level. Loaded into every session —
keep entries short, newest first.

Format: `YYYY-MM-DD — decision — why`

<!-- Budget: 3–5 lines. Record the decision AND the rejected alternative
     with the reason — future sessions re-litigate choices whose "why"
     isn't written down. The full argument (what else was weighed, what
     it costs, what would change the call) goes in learnings.md, and the
     entry points at it: "see learnings YYYY-MM-DD". This file is paid
     for on every session; the argument is read about once. -->
